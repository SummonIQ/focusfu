# Focusfu final clean weighted logo assets

Selected final: the approved F mark is unchanged, `.com` is removed, `focus` is materially heavier, and `fu` is lighter but still part of the same wordmark system.

Recommended files:

- `focusfu-logo.png`
- `focusfu-logo.svg`
- `png/dark-card/focusfu-card-final-clear-weight-split.png`
- `svg/card/focusfu-card-final-clear-weight-split.svg`

Important detail:

- The wordmark is not bitmap-traced. The SVG wordmark is built from real Lato font outline curves, so the curved glyphs render smoothly.
- `focus` uses Lato Black-style outlines.
- `fu` uses Lato Regular-style outlines for a clearly visible contrast.
- The approved F mark is embedded from the approved source image so its look does not change.

Also included:

- `recommended`: focus Black / fu Medium
- `balanced`: focus Heavy / fu Semibold
- `more-contrast`: focus Black / fu Regular, same as selected final
- transparent lockups
- wordmark-only variants
- favicon/app icons
- source-text SVGs for editing/reference
